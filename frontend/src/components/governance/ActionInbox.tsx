import { ArrowUpRight, CircleDot } from "lucide-react";
export type ActionItem = { id: string; title: string; detail?: string; kind?: string };
export function ActionInbox({ items = [], onSelect }: { items?: ActionItem[]; onSelect?: (item: ActionItem) => void }) { return <section className="sc-panel p-5" aria-labelledby="inbox-heading"><div className="mb-4 flex items-center justify-between"><h2 id="inbox-heading" className="text-sm font-semibold">Action inbox</h2><span className="rounded-full bg-sc-warning-soft px-2 py-1 text-xs font-semibold text-sc-warning">{items.length} needs review</span></div>{items.length ? <div className="divide-y divide-sc-line">{items.map(item => <button key={item.id} type="button" onClick={() => onSelect?.(item)} className="flex w-full items-center gap-3 py-3 text-left transition hover:bg-sc-canvas focus-visible:rounded-sc-sm focus-visible:bg-sc-canvas focus-visible:outline focus-visible:outline-2 focus-visible:outline-sc-accent focus-visible:outline-offset-2"><CircleDot className="h-4 w-4 shrink-0 text-sc-accent" /><span className="min-w-0 flex-1"><span className="block text-sm font-semibold">{item.title}</span><span className="block text-xs text-sc-muted">{item.detail ?? item.kind ?? "Human decision required"}</span></span><ArrowUpRight className="h-4 w-4 text-sc-muted" /></button>)}</div> : <p className="text-sm text-sc-muted">Nothing requires a decision right now.</p>}</section>; }
