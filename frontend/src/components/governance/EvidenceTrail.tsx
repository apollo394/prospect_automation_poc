"use client";
import { motion } from "motion/react";
import { FileText, Quote } from "lucide-react";
export type ProspectEvidence = { id?: string; source_type?: string; quote?: string; evidence_status?: string; confidence?: string; synthetic_label?: string };
export type GoverningCitation = { document_id?: string; version?: string; title?: string };
export function EvidenceTrail({ evidence = [], citations = [] }: { evidence?: ProspectEvidence[]; citations?: GoverningCitation[] }) { return <section aria-labelledby="evidence-heading" className="sc-panel p-5"><div className="mb-4 flex justify-between"><h2 id="evidence-heading" className="text-sm font-semibold">Evidence trail</h2><span className="text-[11px] uppercase tracking-[.1em] text-sc-muted">{evidence.length} evidence · {citations.length} citations</span></div><div className="space-y-3">{evidence.map((item, index) => <motion.article key={item.id ?? index} initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .06 }} className="border-l-2 border-sc-accent/40 pl-4"><div className="flex flex-wrap items-center gap-2 text-[11px] font-semibold uppercase tracking-[.1em] text-sc-muted"><Quote className="h-3.5 w-3.5 text-sc-accent" />{item.source_type ?? "Source record"}<span className="rounded-full bg-sc-canvas px-2 py-0.5">{item.evidence_status ?? "Status missing"}</span></div><p className="mt-2 text-sm leading-6">{item.quote ?? "No quoted fact supplied."}</p><p className="mt-2 text-xs text-sc-muted">{item.synthetic_label ?? "Synthetic demonstration data"} · confidence {item.confidence ?? "not stated"}</p></motion.article>)}<div className="border-t border-sc-line pt-3"><div className="mb-2 flex items-center gap-2 text-[11px] font-semibold uppercase tracking-[.1em] text-sc-muted"><FileText className="h-3.5 w-3.5 text-sc-accent" /> Governing citations</div>{citations.length ? citations.map((citation, index) => <p key={`${citation.document_id ?? index}`} className="text-xs text-sc-muted">{citation.title ?? "Governing document"} · {citation.document_id ?? "Document ID missing"} / {citation.version ?? "version missing"}</p>) : <p className="text-xs text-sc-warning">No governing citations supplied.</p>}</div></div></section>; }
