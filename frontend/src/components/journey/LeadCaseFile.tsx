import type { GovernedJourney } from "@/lib/types";

export default function LeadCaseFile({ journey }: { journey: GovernedJourney }) {
  const prospect = journey.prospect;
  const scoreappResult = journey.scoreapp.summary || journey.scoreapp.exact_decision || "ScoreApp qualification evidence is ready for review.";
  return <section className="sc-panel p-5 animate-sc-fade-up"><p className="text-xs font-semibold uppercase tracking-[.14em] text-sc-accent">Lead case file</p><h2 className="mt-2 text-xl font-bold text-sc-ink">ScoreApp qualification signal</h2><p className="mt-3 text-sm leading-relaxed text-sc-charcoal">{String(scoreappResult)}</p><div className="mt-5 grid gap-4 border-t border-sc-line pt-4 sm:grid-cols-3"><div><p className="text-xs text-sc-muted">Prospect</p><p className="mt-1 text-sm font-semibold text-sc-ink">{String(prospect.name || journey.company_name)}</p></div><div><p className="text-xs text-sc-muted">Evidence context</p><p className="mt-1 text-sm font-semibold text-sc-ink">ScoreApp submission + website context</p></div><div><p className="text-xs text-sc-muted">Commercial route</p><p className="mt-1 text-sm font-semibold text-sc-ink">Not yet determined</p></div></div><p className="mt-5 rounded-sc-sm bg-sc-canvas p-3 text-xs leading-relaxed text-sc-muted">Website evidence is contextual input only. ScoreApp identifies possible friction; it does not establish root cause or select an engagement. Prepare Diagnostic is the sole available next action because a human strategist must validate the signal before downstream recommendations.</p></section>;
}
