import type { GovernedArtifact } from "@/lib/types";

export default function DiagnosticBrief({ diagnostic }: { diagnostic: GovernedArtifact }) {
  const questions = diagnostic.governance?.open_questions || [];
  const hypotheses = Array.isArray(diagnostic.hypotheses) ? diagnostic.hypotheses : [];
  return <section className="sc-panel p-5 animate-sc-fade-up"><p className="text-xs font-semibold uppercase tracking-[.14em] text-sc-accent">Evidence-led diagnostic</p><h2 className="mt-2 text-xl font-bold text-sc-ink">{diagnostic.title || "Working hypotheses"}</h2><p className="mt-3 text-sm leading-relaxed text-sc-charcoal">{diagnostic.summary || "Review the supplied evidence before importing the transcript."}</p><div className="mt-5 grid gap-5 md:grid-cols-2"><div><div className="flex items-center justify-between"><h3 className="text-sm font-semibold text-sc-ink">Working hypotheses</h3><span className="text-xs text-sc-muted">To validate</span></div>{hypotheses.length ? <ul className="mt-3 space-y-2">{hypotheses.map((hypothesis) => <li key={String(hypothesis)} className="rounded-sc-sm border-l-2 border-sc-accent/50 bg-sc-canvas px-3 py-2 text-sm leading-6 text-sc-charcoal">{String(hypothesis)}</li>)}</ul> : <p className="mt-2 text-sm text-sc-muted">No structured hypothesis evidence was supplied.</p>}</div><div><h3 className="text-sm font-semibold text-sc-ink">Conversation guide</h3>{questions.length ? <ol className="mt-3 space-y-2">{questions.map((question, index) => <li key={question} className="flex gap-3 text-sm leading-6 text-sc-charcoal"><span className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-sc-canvas text-xs font-semibold text-sc-primary">{index + 1}</span>{question}</li>)}</ol> : <p className="mt-2 text-sm text-sc-muted">No open questions supplied by the fixture.</p>}</div></div><p className="mt-5 border-t border-sc-line pt-3 text-xs text-sc-muted">Evidence status: synthetic demonstration data · Confidence informs review priority; it does not make the decision.</p></section>;
}
