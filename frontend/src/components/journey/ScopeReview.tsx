import { ClipboardList } from "lucide-react";
import type { GovernedArtifact } from "@/lib/types";
export default function ScopeReview({ scope }: { scope: GovernedArtifact }) { const list=(k:string)=>(Array.isArray(scope[k])?scope[k] as unknown[]:[]); const phases=list("phases") as Array<Record<string,unknown>>; return <div className="space-y-5"><section className="sc-panel p-5"><div className="flex gap-3"><ClipboardList className="h-5 w-5 text-sc-accent"/><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-sc-accent">Route-specific scope</p><h2 className="mt-1 text-xl font-semibold text-sc-ink">{String(scope.title??"Proposed engagement scope")}</h2><p className="mt-3 text-sm leading-6 text-sc-charcoal">{String(scope.summary??"Scope draft awaiting reviewer validation.")}</p></div></div></section><section className="sc-panel p-5"><h2 className="font-semibold text-sc-ink">Milestones and deliverables</h2><div className="mt-4 space-y-4">{phases.map((p,i)=><div key={String(p.id??i)} className="border-l-2 border-sc-accent/40 pl-4"><div className="flex justify-between"><h3 className="font-medium text-sc-ink">{String(p.name??`Phase ${i+1}`)}</h3><span className="text-xs text-sc-muted">{String(p.duration??"Timing to confirm")}</span></div><ul className="mt-2 list-disc pl-5 text-sm text-sc-charcoal">{(Array.isArray(p.deliverables)?p.deliverables:[]).map(x=><li key={String(x)}>{String(x)}</li>)}</ul></div>)}</div></section>{(["inclusions","exclusions","assumptions","dependencies","client_responsibilities","open_questions"] as const).map(k=><section key={k} className="sc-panel p-5"><h2 className="font-semibold text-sc-ink">{k.replaceAll("_"," ")}</h2><ul className="mt-3 list-disc pl-5 text-sm leading-6 text-sc-charcoal">{list(k).length?list(k).map(x=><li key={String(x)}>{String(x)}</li>):<li className="list-none -ml-5 text-sc-muted">No governed items supplied.</li>}</ul></section>)}</div>; }
